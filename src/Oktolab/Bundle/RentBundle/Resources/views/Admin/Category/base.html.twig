{% extends 'OktolabRentBundle:Admin:layout.html.twig' %}
{% import 'OktolabRentBundle::aui_tools.html.twig' as macro %}

{% set highlight_action = "category" %}

{% block content %}
    <h1 class="admin-heading">{{'inventory.categories'|trans}}</h1>
    <p class="admin-subtitle">{{'admin.category.about'|trans}}</p>

    <div class="aui-toolbar2">
        <div class="aui-toolbar2-inner">
            <div class="aui-toolbar2-primary">
                <div id="button-set1" class="aui-buttons">
                    <a class="aui-button" href="{{ path('inventory_category') }}">{{'generic.overview'|trans}}</a>
                    <a class="aui-button" href="{{ path('inventory_category_new') }}">{{'inventory.category.new'|trans}}</a>
                </div>
            </div>
        </div>
    </div>
{% endblock %}